# Screenshot Checklist

| Asset filename | Route / screen | Seeded scenario | What to highlight | Crop / framing guidance | Static or GIF | Capture prerequisites |
| --- | --- | --- | --- | --- | --- | --- |
| `docs/demo-assets/01-dashboard-overview.png` | `/dashboard` | Fresh seed | KPI cards, SLA health, recent routing decisions | Keep the KPI row and the first analytics row above the fold. Avoid including browser bookmarks. | Static | Run `npm run db:seed` first. |
| `docs/demo-assets/02-request-demo-ingest.png` | Terminal or API client beside the app | Atlas Grid `request_demo` | The exact `POST /api/signals` request and the matched response payload | Use a split view if possible. Keep the JSON response visible, especially `outcome`, `signalId`, and matched entities. | Static | Use a unique `submission_id`. |
| `docs/demo-assets/03-atlas-grid-account-detail.png` | `/accounts/acc_atlas_grid` | Atlas Grid | Account header, score, and open tasks | Capture the account header and the first part of the right column in one frame. | Static | Refresh after ingest if you want the newest signal visible. |
| `docs/demo-assets/03b-atlas-grid-score-breakdown.png` | `/accounts/acc_atlas_grid` | Atlas Grid | Score breakdown component list | Crop tightly around the score breakdown card so the reason-code-driven components are readable. | Static | None beyond fresh seed. |
| `docs/demo-assets/03c-atlas-grid-audit-log.png` | `/accounts/acc_atlas_grid` | Atlas Grid | Audit log entries for score, route, SLA, and task traces | Crop the audit log card only. Keep timestamps and explanation text visible. | Static | Best captured after the live ingest step. |
| `docs/demo-assets/04-routing-simulator.png` | `/routing-simulator` | Matching Atlas-style routing context | Decision type, owner, queue, and evaluated policy steps | Keep both the scenario inputs and the result/explanation in frame if they fit cleanly. | Static | Use a scenario that shows clear precedence or fallback. |
| `docs/demo-assets/05-dashboard-closeout.png` | `/dashboard` | Fresh seed after demo flow | Benchmark metrics, unmatched backlog, and routing feed | Use a wider frame than the opening shot so the closing metrics feel distinct. | Static | Refresh after the live ingest if you want the newest routing row visible. |
| `docs/demo-assets/urgent-inbound-flow.gif` | `/dashboard` to `/accounts/acc_atlas_grid` | Atlas Grid | Short ingest-to-account-detail motion | Keep the clip under 10 seconds. Show dashboard, terminal request, and Atlas account detail only. | GIF | Reset the seed before recording to avoid noisy prior state. |
